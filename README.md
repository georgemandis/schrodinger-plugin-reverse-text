# Reverse Text Plugin for Schrödinger

Reverses the text content on the clipboard.

## Installation

Install from the Schrödinger Plugin Manager (Preferences → Plugins → Browse).

To install manually or develop locally, symlink the plugin into your plugins directory:

```sh
ln -s "$(pwd)" ~/.schrodinger/plugins/reverse-text
```

## License

MIT
